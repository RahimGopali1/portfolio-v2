# Resume

Place the downloadable resume PDF in this folder as:

    rahim-gopali-resume.pdf

The download path is centralised in `src/app/shared/data/career.data.ts`
(constants `RESUME_PDF_PATH` and `RESUME_PDF_FILENAME`). If you rename the
file, update `RESUME_PDF_PATH` there and the sidebar button picks it up.
